<div id="error_explanation">
  <div class="alert alert-danger">
    <strong><%= pluralize(object.errors.uniq.count, "error") %> prohibited this website from being saved:</strong>
    <hr>
    <ul>
      <% object.errors.full_messages.uniq.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
</div>